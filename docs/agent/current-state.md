# Current state

Updated 2026-09-08. Foundation / prototype stage; initial repository was empty with no commits.

Implemented: two authored Unity scenes and stable metadata; primitive bird, ground and three perches; shared FlightInputFrame/IFlightInput; XR/gamepad/synthetic providers; pure kinematic BirdFlightController; runtime driver/camera/diagnostics; deterministic EditMode and scene-wiring PlayMode tests; explicit URP/OpenXR/Android editor setup and development build methods; Blender static-mesh validation/export/roundtrip scripts; offline tools, Git ignores/LFS patterns and development/design/agent docs.

Editor baseline: Unity 6000.6.0f1 is the actual local install and `ProjectVersion.txt`'s pin was updated to match (was 6000.3.21f1, never installed on this machine); URP/Input System/XR Management/OpenXR/Test Framework versions resolved by Package Manager against that editor, recorded in `unity/Packages/packages-lock.json`. `tools/scripts/tool_discovery.py` now finds Unity/Blender/adb by Hub layout, override env var or PATH and reports version mismatches instead of guessing; `doctor.py`/`unity.py` use it. Blender authoring recommendation: 4.5 LTS (unchanged, still unverified on this machine — see below).

Passed: offline structural checks, host Python tooling tests, Unity YAML syntax/reference inspection and whitespace review, **and now real editor validation**: Configure Foundation ran and generated ProjectSettings/URP/XR assets and the package lockfile; the project compiles with no console errors/warnings beyond a harmless Unity Account API timeout and an MCP CLI-detection notice; `BootstrapSmokeTests.BootstrapLoadsConnectedPrototype` passes in PlayMode (also checks every renderer has a supported shader/material); manually entering Play mode in the Editor drives the bird forward under synthetic input (~3 m/s, confirmed by world-Z movement over several seconds and the on-screen `FlightDiagnostics` HUD), with ground and perches rendering via Unity MCP screenshots. Blender/Git LFS were not exercised this session; FBX roundtrip, Android build and Quest hardware validation remain **unrun**.

No aerodynamics, collision/perching, stamina, food, AI, finished gamepad UX, custom Meta SDK features or Switch tooling. Existing input and camera mappings are provisional. Desktop diagnostics are disabled in XR. Graphics are placeholder primitives (boxes/planes), not final art. No third-party art, downloaded binaries, commits or pushes.

Next: install/locate Blender and run its smoke test, install Android Build Support to unblock `ConfigureXR`'s Android branch (currently skipped — desktop-only foundation), then authorize Quest USB and make the first development build. See [foundation handoff](session-handoffs/2026-09-08-foundation.md) and [first-playtest handoff](session-handoffs/2026-09-08-first-playtest.md).
